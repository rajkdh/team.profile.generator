const buildTeam = team => {
    const generateManager = manager => {
        return `
        <div class="profile-card" id="manager-display">
        
        </div>
        
        
        `
    }
}